# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Coverage artifacts
backend/.coverage
flutter/coverage/

# Flutter
flutter/build/
flutter/.dart_tool/
flutter/.packages
flutter/.flutter-plugins
flutter/.flutter-plugins-dependencies
# pubspec.lock is intentionally committed (this is an app, not a library)

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Data (keep structure, ignore large exports)
data/*.json
!data/.gitkeep

# Generated DB and exports
*.db
*.db-shm
*.db-wal
*.db-journal
backend/db/wayfarer-hotels-gmaps.csv

.env
.env.*
!.env.example

# Google OAuth credentials and tokens — never commit
backend/secrets/*
!backend/secrets/.gitkeep
/secrets/

# MCP config may embed API keys
.mcp.json

.worktrees/
.claude/worktrees/
